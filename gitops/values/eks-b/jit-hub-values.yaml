# gitops/values/eks-b/jit-hub-values.yaml
# 평시 Cold Standby입니다. JIT 전환 시 replicaCount를 올리고 gateway도 활성화합니다.
gateway:
  replicaCount: 1
auth:
  replicaCount: 1
  db:
    enabled: true
    host: "postgres-gateway"
weather:
  replicaCount: 1
  db:
    enabled: true
    host: "postgres-gateway"
traffic:
  replicaCount: 1
  db:
    enabled: true
    host: "postgres-gateway"
tourist:
  replicaCount: 1
  db:
    enabled: true
    host: "postgres-gateway"
